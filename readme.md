<!-- 
Modulo 2: 
- Curso 02 - HTML e CSS: Classes, Posicionamento e FlexBox:
    
    * O que foi aprendido/perceber neste código:
    01- importância de resetar o padrão do CSS;
    --- Box model (margin, border e padding);
    --- Diferentes seletores CSS. (. ; # ;tag)
        
    02- Padrões de projetos CSS com height e box-sizing;
    --- Unidade Viewport; (vh ou vw)
    --- Flexbox. (display: flex e alinhamentos)
    
    03- Criar seções (sections) no HTML; (HTML semântico)
    --- Posicionar elementos com Flexbox utilizando o justify-content;
    --- Boas práticas para nomear classes; (apresentacao__conteudo__titulo = separados por __)
    --- Alterar o tamanho, tipo e peso da fonte com font-size, font-family e font-weight;
    --- Importar fontes com Google Fonts.
    
    04- Posicionar e estilizar botões;
    --- Criar e utilizar divisões (divs) no projeto;
    --- Retirar o sublinhado com o text-decoration;
    --- Arredondar bordas com border-radius.

    05- Utilizar o recurso Gap para espaçar cada elemento filho.

- Curso 03 - HTML e CSS: Cabeçalho, Footer e Variáveis CSS:
    
    * O que foi aprendido/perceber neste código:
    01- Posicionar os botões conforme o novo layout através do display: flex; (utilizando align-items)
    (**Use justify-content to align along the main axis. Use align-items to align along the cross axis.**)
    
    03- Usando :hover para animações

    04- Navegação entre telas

    05- Variáveis CSS (:root {[--nome: props]}) e utilização: (bg-color: var([nome])) 


- Curso 04 - HTML e CSS: Responsividade e publicação do projeto:
    
    * O que foi aprendido/perceber neste código:
    01- unidade REM (utilizado para fontes, de acordo com :root da página = 1rem = 16px / 2rem = 32px- medida relativa que possibilita o navegador alterar o tamanaho da fonte);

    02- Uso de porcentagem (a porcentagem é de acrodo com a tag pai) e Dev Tools para mudar tamanho de dispositivo

    03- Media Queries ( @media (max-width: 960px) {})

    04- Github

    05- Projeto no ar com Vercel

-->